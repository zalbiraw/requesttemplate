displayName: Request Template
type: middleware
iconPath: .assets/icon.png

import: github.com/zalbiraw/requesttemplate

summary: 'Allows user to define a json template for the request'

testData:
  commands: 
    - .user.message |= "hello, " + .